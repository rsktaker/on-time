import Foundation

enum AppConstants {
    static let appName = "On Time"
    static let shortcutName = "On Time"

    // Replace this with your iCloud share URL after publishing the Shortcut once
    // (Shortcuts app → On Time → Share → Copy iCloud Link).
    static let shortcutInstallURL = "https://github.com/rsktaker/on-time#install-the-shortcut"

    static let contactEmail = "ruchirkavulli@gmail.com"
    static let contactPhone = "4084656442"
    static let supportURL = "https://github.com/rsktaker/on-time"

    static let defaultBufferMinutes = 2
    static let minBufferMinutes = 1
    static let maxBufferMinutes = 30
}
